// 인물별 '실제 주요 사역지' 좌표 오버라이드.
// 서울(정동)에 14명이 한 점에 몰려 원형으로 흩어지던 것을, 각자 활동한 실제
// 장소(정동제일교회·이화·배재·새문안·세브란스·연동교회·연희전문 등)로 고증 배치한다.
// 좌표는 현존 사적·교회·학교 위치 기준의 근사값. 값이 없는 인물은 기존 분산 방식 사용.

export const PERSON_COORD: Record<string, [number, number]> = {
  // ── 제중원(광혜원) 계열: 재동~구리개 ──
  allen: [37.5772, 126.985], // 광혜원·제중원(재동)
  heron: [37.5766, 126.9856], // 제중원 2대 원장
  // ── 정동 선교 구역 ──
  appenzeller: [37.5659, 126.9737], // 정동제일교회·배재
  mscranton: [37.5664, 126.9729], // 이화학당
  bunker: [37.5649, 126.9724], // 배재학당·육영공원
  annie: [37.5673, 126.9748], // 정신여학교(정동 시기)
  hulbert: [37.5668, 126.9756], // 육영공원(정동 인근)
  // ── 새문안(서대문) ──
  underwood: [37.5702, 126.9728], // 새문안교회
  lillias: [37.5699, 126.9742], // 언더우드와 동역
  // ── 상동(남대문 안) ──
  william: [37.5608, 126.9791], // 상동교회
  // ── 세브란스(남대문 밖 도동) ──
  avison: [37.5556, 126.9722], // 세브란스병원·의학교
  schofield: [37.5562, 126.9731], // 세브란스 교수
  // ── 연동(동부) ──
  gale: [37.5722, 126.9989], // 연동교회
  // ── 연희전문(서부 신촌) ──
  hhunderwood: [37.5665, 126.9387], // 연희전문학교
  // ── 추가 대표 인물(서울권 실제 사역지) ──
  moore: [37.5714, 126.9861], // 승동교회(곤당골)
  campbell: [37.5780, 126.9690], // 배화학당(필운동)
  aliceappenzeller: [37.5651, 126.9710], // 이화학당(정동)
  soda: [37.5460, 126.9780], // 가마쿠라보육원(후암동)
  maclay: [37.5760, 126.9770], // 고종 알현(경복궁 인근)
};
